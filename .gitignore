# Stuff that pops up locally for me - @jeremyfelt
*.sublime*
.DS_Store
/.idea

# Because this really is a working directory, ignore vagrant's files
/.vagrant
/www/wp.wsu.edu/content/uploads/
# No need to share our mysql data with each other
/database/data/*

# And no need to share individual SQL files with each other
*.sql
*.sql.gz

# BUT....

# we do have default SQL files that should be included
!/database/init.sql
!/database/stages/*.sql

/www/wp.wsu.edu/.htaccess
/www/wp.wsu.edu/content/log/*.log
/www/wp-cli
